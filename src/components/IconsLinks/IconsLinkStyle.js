import styled from "styled-components";

export const IconsContainer = styled.div`
  display: flex;
  gap: 16px;
  justify-content: center;
  margin-top: 24px;

  a:hover {
    transform: scale(1.2);
  }
`;
